from setuptools import setup, find_packages

setup(
    name="energy_simulations",
    version="0.1",
    packages=find_packages(),  # Automatically find all sub-packages
    install_requires=[],       # Add dependencies here if needed
)
